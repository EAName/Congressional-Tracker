# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
.env
.env.*
!.env.example

# uv / tooling
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
.dmypy.json
dmypy.json

# Immutable raw landing zone and local warehouse (regenerated by ingest)
data/raw/**
!data/raw/.gitkeep
data/warehouse/**
!data/warehouse/.gitkeep

# data/votes.csv is committed — Prompt 1 adjudication audit trail.
# data/derived/irt.json is committed — Prompt 4 offline 2PL artifact.

# Reports: keep directory; commit coverage/diffs when CI is green (Prompt 10)
data/reports/**
!data/reports/.gitkeep
!data/reports/*.md

*.duckdb
*.duckdb.wal

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Secrets
credentials.json
service-account*.json
**/secrets/
