/** @type {import('next').NextConfig} */
const nextConfig = {
  // All pages are statically generated from imported JSON — no server data layer.
  reactStrictMode: true,
};

export default nextConfig;
